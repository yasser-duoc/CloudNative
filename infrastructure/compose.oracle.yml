services:
  oracle:
    image: gvenzl/oracle-free:23-slim
    container_name: oracle
    environment:
      ORACLE_PASSWORD: ${ORACLE_PASSWORD:-digitalfix}
      APP_USER: ${ORACLE_USERNAME:-digitalfix}
      APP_USER_PASSWORD: ${ORACLE_PASSWORD:-digitalfix}
    ports:
      - "1521:1521"
    volumes:
      - oracle-data:/opt/oracle/oradata
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 20
    restart: unless-stopped
    networks:
      - digitalfix-net

volumes:
  oracle-data:

networks:
  digitalfix-net:
    external: true
